import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'hello-goodbye-ionic-angular-v1',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
